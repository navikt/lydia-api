# How-to

1. Lag et virtuelt miljø:
    - `python3.12 -m venv env`
2. Ta i bruk miljø:
    - `source env/bin/activate`
3. Installer avhengigheter i miljø:
    - `pip3 install -r requirements.txt` 
4. Kjør python script for å generere:
    - `python3 generer_sql.py`

Kjør tester med:
   - `pytest`